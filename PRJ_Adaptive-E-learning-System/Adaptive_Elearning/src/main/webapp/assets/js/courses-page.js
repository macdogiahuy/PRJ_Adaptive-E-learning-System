// Page specific JS for courses page
// Currently empty - reserved for future interactive behaviors (filters, ajax pagination)
(function(){
    // Example: log when page loads
    if (typeof console !== 'undefined') console.log('courses-page.js loaded');
})();

// Position stats card to remain fixed on large screens
// No dynamic positioning required: using CSS position:sticky for the sidebar stats card.
// Placeholder for future interactive behavior.
(function(){
    if (typeof console !== 'undefined') console.log('courses-page.js loaded (sticky sidebar enabled via CSS)');
})();