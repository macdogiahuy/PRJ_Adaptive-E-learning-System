# Database Configuration Template
# Copy this file to env.properties and fill in your actual values

# Database URL - change localhost, instance name, and database name as needed
DB_URL=jdbc:sqlserver://localhost\\YOUR_SQL_INSTANCE;databaseName=YOUR_DATABASE_NAME;encrypt=true;trustServerCertificate=true;

# Database credentials
DB_USER=your_username
DB_PASSWORD=your_password

# Google OAuth Configuration
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API or People API
# 4. Go to "Credentials" and create OAuth 2.0 Client ID
# 5. Add your redirect URI (e.g., http://localhost:8080/YourApp/auth/google/callback)
GOOGLE_CLIENT_ID=REDACTED
GOOGLE_CLIENT_SECRET=REDACTED
REDIRECT_URI=http://localhost:8080/YourAppName/auth/google/callback