Đặt các file chứng chỉ TLS (fullchain.pem, privkey.pem) được phát hành bởi Let’s Encrypt hoặc CA tin cậy vào thư mục này trước khi chạy docker-compose. Không commit các file chứng chỉ thật vào repo.

